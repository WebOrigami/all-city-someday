// This file defines the overall structure of the site.
{
  // Stylesheet, etc.
  <assets>
  
  // The home page shows all the photos.
  index.html = <index.jse>(photos)

  // Include all the photo files.
  photos/ = <photos.jse/>

  // JSON and RSS feeds
  feed.json = Tree.json(<feed.jse>(photos))
  feed.xml = Origami.rss(<feed.jse>(photos))
}
