// This file defines the overall structure of the site.
{
  // Stylesheet, etc.
  assets
  
  // The home page shows all the photos.
  index.html = index.ori(photos)

  // Include all the photo files.
  photos = photos.ori/

  // JSON and RSS feeds
  feed.json = @json(feed.ori(photos))
  feed.xml = @rss(feed.ori(photos))
}
